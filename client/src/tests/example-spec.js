import { expect } from 'chai';

/*eslint-disable */
describe('Testing', () => {
    it('should pass', () => {
        expect(1).to.be(1);
    });
    it('should fail', () => {
        expect(1).to.be(2);
    });
});
/*eslint-enable */
